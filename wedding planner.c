#include<stdio.h>#include<stdlib.h>#define people_per_coke_case 6#define people_per_water_case 2#define cost_coke 5#define cost_water 3int mchoice();   /*menu choice */int checking();  int guests();int card_sweets(int,int*,int*);int tables(int,int *);int coke_water(int, int *, int *);int cost_of_cards(int,int *,int*);int cost_of_drinks(int, int,int *,int*);int display(int,int*,int*,int*,int*,int*,int*,int*,int*);int main(){int a=0,choice=0,guest=0,card=0,sweet=0,table=0,coke=0,water=0,cost_card=0,costcoke=0,costwater=0;printf("   Wedding Planning Assistant is pleased to be at your service!\n");while(a==0){mchoice();choice=checking();switch(choice){case 1:guest=guests();
break;case 2:card_sweets(guest, &card, &sweet);break;case 3:tables(guest, &table);break;case 4:coke_water(guest,&coke,&water);break;case 5:cost_of_cards(guest,&card,&cost_card);break;case 6:cost_of_drinks(water,coke,&costcoke,&costwater);break;case 7:display(guest,&card,&sweet,&table,&coke,&water,&cost_card,&costcoke,&costwater);break;case 8:a=1;}}}int mchoice(){  /* it will print menu */printf("1. Enter number of invited guests.\n2. Determine the number of invitation cards and sweets\n");printf("3. Determine the number of tables needed. \n4. Determine drinks order \n5. Cost of invitation cards\n");printf("6. Cost of drinks. \n7. Display all information \n8. Quit.\n");}int checking(){  /* this function gets menu choice and returns it to main */int choice;scanf("%d",&choice);while(choice>8 || choice<0){scanf("%d",&choice);
printf("Error, please enter again..\n");}return choice;}int guests(){ /* it gets number of guests */int guest;printf("Enter number of guests: \n");scanf("%d",&guest);return guest;}int card_sweets(int guest, int *card, int *sweet){if(guest==0){printf("\nChoose option 1 first\n");}else{*card=guest/2;*sweet=guest*1.2;}}int tables(int guest,int *table){if(guest==0){printf("\nChoose option 1 first\n");}else{if(guest%6==0){*table=guest/6;}
else{*table=guest/6+1;}}}int coke_water(int guest,int *coke,int *water){ /* number of cokes and water */if(guest==0){printf("\nChoose option 1 first\n");}else{*coke=guest/people_per_coke_case;if(guest%6==0){*water=guest/people_per_water_case;}else{*water=guest/people_per_water_case+1;}}} int cost_of_cards(int guest,int *card,int *cost_card){if(guest==0){printf("\nChoose option 1 first then menu option 2");}*cost_card=*card*10;}int cost_of_drinks(int water, int coke,int *costcoke, int *costwater){if(water==0){printf("\nChoose option 1 first then option 4");}
*costcoke=cost_coke*coke;*costwater=cost_water*water;}int display(int guest,int *card,int *sweet,int *table,int *coke,int *water,int *cost_card,int *costcoke, int *costwater){printf("Enter the number of invited guests: %d\n",guest);printf("Needs: %d invitation cards ,%d sweets, %d tables, %d cases_of_coke, %d cases_of_water\n",*card,*sweet,*table,*coke,*water);printf("Cost of invitation cards: %d\n",*cost_card);printf("Cost of drinks: %d for coke, %d for water for water with a total of %d\n",*costcoke,*costwater,*costcoke+*costwater);}
